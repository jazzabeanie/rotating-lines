# Agent Instructions

- Always work on a feature branch, never commit directly to `main`. If the current branch is `main`, create a new feature branch before making any changes.
- Whenever you are on a feature branch (i.e. not `main`), commit and push changes after completing each change without waiting to be asked.
- On `main`, do not commit or push without being explicitly asked.
